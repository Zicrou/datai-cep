<main class="col-sm-9 offset-sm-3 col-md-10 offset-md-2 pt-3">
  <%= form_with(model: conjoint, local: true) do |form| %>
    <% if conjoint.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(conjoint.errors.count, "error") %> prohibited this conjoint from being saved:</h2>

        <ul>
        <% conjoint.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="form-group">
      <%= form.label :nom %>
      <%= form.text_field :nom, class:"form-control col-md-3"%>
    </div>

    <div class="form-group">
      <%= form.label :prenom %>
      <%= form.text_field :prenom, class:"form-control col-md-3"%>
    </div>

    <div class="form-group">
      <%= form.label :photo_certificat_mariage %>
      <%= form.text_field :photo_certificat_mariage, class:"form-control col-md-3"%>
    </div>

    <div class="form-group" style="display:none;">
      <%= form.label :agent_id %>
      <%= form.text_field :agent_id %>
    </div>

    <div class="actions">
      <%= form.submit %>
    </div>
  <% end %>

</main>