<main class="col-sm-9 offset-sm-3 col-md-10 offset-md-2 pt-3">
  <%= form_with(model: marier, local: true) do |form| %>
    <% if marier.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(marier.errors.count, "error") %> prohibited this marier from being saved:</h2>

        <ul>
        <% marier.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= form.label :name %>
      <%= form.text_field :name %>
    </div>

    <div class="actions">
      <%= form.submit %>
    </div>
  <% end %>
</main>