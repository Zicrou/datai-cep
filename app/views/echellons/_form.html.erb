<main class="col-sm-9 offset-sm-3 col-md-10 offset-md-2 pt-3">
  <%= form_with(model: echellon, local: true) do |form| %>
    <% if echellon.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(echellon.errors.count, "error") %> prohibited this echellon from being saved:</h2>

        <ul>
        <% echellon.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="form-group">
      <%= form.label :code %>
      <%= form.text_field :code, class:"form-control col-md-3" %>
    </div>

    <div class="actions">
      <%= form.submit value:"Ajouter un Echellon", class:"btn btn-primary" %>
    </div>
  <% end %>
</main>