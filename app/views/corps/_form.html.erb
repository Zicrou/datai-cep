<main class="col-sm-9 offset-sm-3 col-md-10 offset-md-2 pt-3">
  <%= form_with(model: corp, local: true) do |form| %>
    <% if corp.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(corp.errors.count, "error") %> prohibited this corp from being saved:</h2>

        <ul>
        <% corp.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="form-group">
      <%= form.label :code %>
      <%= form.text_field :code, class:"form-control col-md-3" %>
    </div>

    <div class="form-group">
      <%= form.label :name %>
      <%= form.text_field :name, class:"form-control col-md-3" %>
    </div>

    <div class="actions">
      <%= form.submit value:"Ajouter un corps", class:"btn btn-primary" %>
    </div>
  <% end %>
</main>