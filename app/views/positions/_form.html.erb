<%= form_with(model: position, local: true) do |form| %>
  <% if position.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(position.errors.count, "error") %> prohibited this position from being saved:</h2>

      <ul>
      <% position.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :code %>
    <%= form.text_field :code %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :date_debut_position %>
    <%= form.date_select :date_debut_position %>
  </div>

  <div class="field">
    <%= form.label :date_fin_position %>
    <%= form.date_select :date_fin_position %>
  </div>

  <div class="field">
    <%= form.label :date_annulation_position %>
    <%= form.date_select :date_annulation_position %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
