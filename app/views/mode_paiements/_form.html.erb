<%= form_with(model: mode_paiement, local: true) do |form| %>
  <% if mode_paiement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mode_paiement.errors.count, "error") %> prohibited this mode_paiement from being saved:</h2>

      <ul>
      <% mode_paiement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
